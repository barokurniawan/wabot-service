version: '3'
services:
  app:
    build:
      context: ./
      dockerfile: docker/app.dockerfile
    image: wabot_app
    container_name: wabot_app
    ports:
        - "8080:8080"
    volumes:
      - ./:/var/www
    restart: always
